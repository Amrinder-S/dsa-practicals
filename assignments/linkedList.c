#include<stdio.h>
struct node {
    int data;
    struct node *next;
};
struct node *head;
int main() {
    printf("Linked List example\n");
    int n, i;
    printf("Enter the number of nodes: ");
    scanf("%d", &n);
    printf("Number of nodes to be entered: %d", n);

    for(i = 0; i < n; i++) {
        
    }
}